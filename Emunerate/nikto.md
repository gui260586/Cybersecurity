# Exemple avec une authetifcation basic
```bash
nikto -h x.x.x.x -id user:password 
```

Explication des arguments
  - -h => Url ou IP du site à attaquer
  - -id => Username:password à utiliser pour attaque le site en basic auth
